import 'package:flutter/material.dart';

class PdfGenerate extends StatelessWidget {
  const PdfGenerate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
